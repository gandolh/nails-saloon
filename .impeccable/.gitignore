# Impeccable — keep shared config, ignore per-session/live state.

# Transient live-mode + critique session state
live/sessions/
live/manual-edits/
live/*.log
critiques/

# Keep version-controlled project config
!live/config.json
!.gitignore
